# SQL-DATAWAREHOUSE-PROJECT
This project demonstrates how to build a modern data warehouse using an end-to-end ETL pipeline designed around the Medallion Architecture (Bronze, Silver, Gold layers). It shows how raw data can be incrementally transformed into high-quality, analytics-ready datasets.
